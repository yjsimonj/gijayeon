gradio>=4.44
huggingface_hub>=0.25

# spaces 는 여기 적지 않는다. ZeroGPU Space는 빌드할 때 pip 명령에 spaces==0.51.1 을
# 직접 붙여서 돌리므로(빌드 로그에서 확인), 여기서 버전을 요구하면 그 핀과 충돌해
# pip이 죽는다 — 실제로 spaces>=0.51.3 으로 BUILD_ERROR 를 냈다.
# torch 도 플랫폼이 "torch<=2.11.0" 으로 같이 깐다. 우리 코드는 둘 다 요구하지 않는다.
